<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/userLogin">
    <fragment
        android:id="@+id/userLogin"
        android:name="com.builditmyself.collectionsview.UserLoginFragment"
        android:label="Login"
        tools:layout="@layout/fragment_user_login" >
        <action
            android:id="@+id/action_userLogin_to_userDetails"
            app:destination="@id/userDetails" />
    </fragment>
    <fragment
        android:id="@+id/userDetails"
        android:name="com.builditmyself.collectionsview.UserDetails"
        android:label="fragment_user_details"
        tools:layout="@layout/fragment_user_details" />
</navigation>